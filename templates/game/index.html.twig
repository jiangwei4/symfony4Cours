{% extends 'base.html.twig' %}


{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">



        {% for game in games %}
            <p>Donc {{ game.firstname }} {{ game.lastname }}
              est née le {{ game.birthday |date("m/d/Y") }}
                et son email est {{ game.email }} <br></p>
        {% endfor %}
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <p>EMAIL: {{ app.user.email }} <a href="\logout">logout</a>   <a href="\profile">modifier profile</a></p>
    {% else %}
        <p>Welcome <a href="\login">login</a> </p>
    {% endif %}
</div>
{% endblock %}
